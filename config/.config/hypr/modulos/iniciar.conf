###############
### INICIAR ###
###############



exec-once = /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1

exec-once = $bar

exec-once = hyprctl setcursor catppuccin-mocha-peach-cursors 26

exec-once = kbuildsycoca6
exec-once = kdeconnectd

exec-once = wl-paste --type text --watch cliphist store # Stores only text data
exec-once = wl-paste --type image --watch cliphist store # Stores only image data

exec-once = swayosd-server
exec-once = swaync

exec-once = sleep 0.5 && hyprctl reload

# Inicie automaticamente os processos necessários (como daemons de notificações, barras de status, etc.)
# Ou execute seus aplicativos favoritos na inicialização, assim:

# exec-once = $terminal
# exec-once = nm-applet &
# exec-once = waybar & hyprpaper & firefox
